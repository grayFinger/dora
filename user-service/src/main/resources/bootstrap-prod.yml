spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://127.0.0.1:8848
        service: ${spring.application.name}
      config:
        server-addr: http://127.0.0.1:8848
        file-extension: yaml
logging:
  level:
    root: info